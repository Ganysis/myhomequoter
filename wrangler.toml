# Cloudflare Pages Configuration
# This file configures Cloudflare Pages for deployment

name = "myhomequoter"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Environment variables (set in Cloudflare dashboard for secrets)
[vars]
PUBLIC_SITE_URL = "https://myhomequoter.com"

# Bindings would be configured in Cloudflare dashboard
# D1 Database, KV Storage, etc.
